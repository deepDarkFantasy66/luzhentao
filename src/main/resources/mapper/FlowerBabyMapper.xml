<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//myBatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dao.FlowerBabyDao">

    <insert id="saveFlowerBaby" parameterType="com.example.bean.FlowerBaby">
        insert into demo values (#{name},#{age})
    </insert>
    <insert id="saveAction" parameterType="com.example.bean.Action">
        insert into action values (#{time},#{action})
    </insert>
</mapper>